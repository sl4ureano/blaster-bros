// Global tuning points for future games built with this engine.
window.PlatformEngineConfig = window.PlatformEngineConfig || {};
window.PlatformEngineConfig.engine = {
  maxPlayers: 4,
  targetFps: 60,
  gravity: 0.72,
  tileSize: 40,
  defaultRoomPort: 3000
};
